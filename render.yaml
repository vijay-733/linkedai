services:
  - type: web
    name: linkedai
    runtime: node
    rootDir: .
    buildCommand: cd frontend && npm ci && node ./node_modules/vite/bin/vite.js build && cd ../backend && npm ci
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false
